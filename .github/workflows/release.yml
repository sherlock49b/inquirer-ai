name: Release

on:
  push:
    branches: [main]
    tags: ["v*"]

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  auto-bump:
    if: github.ref_type == 'branch'
    runs-on: ubuntu-latest
    outputs:
      bumped: ${{ steps.bump.outputs.bumped }}
      version: ${{ steps.bump.outputs.version }}
    steps:
      # Check out (and later push the bump commit + tag) with a PAT, not the
      # default GITHUB_TOKEN. main is protected by a ruleset requiring status
      # checks; the github-actions bot cannot be exempted on a personal repo,
      # but the PAT pushes as the repo owner (admin), who is in the ruleset
      # bypass list — so the auto-bump push to main is allowed.
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PAT }}

      - uses: astral-sh/setup-uv@v7

      - uses: dtolnay/rust-toolchain@stable

      - name: Install commitizen with the cz-agentic plugin
        # cz-agentic drives bumps from feat/fix/refactor/perf and `!` / BREAKING
        # CHANGE. It is not yet on PyPI, so it is installed from GitHub; ./python
        # provides the local inquirer-ai that cz-agentic depends on.
        run: uv tool install commitizen --with ./python --with "git+https://github.com/sherlock49b/cz-agentic.git"

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump version
        id: bump
        run: |
          # Run cz bump (uses [tool.commitizen] name = "cz_agentic" from pyproject.toml).
          # --no-verify so the generated "bump:" commit is not blocked by hooks.
          set +e
          cz bump --yes --changelog --no-verify 2>&1 | tee /tmp/bump.log
          rc=${PIPESTATUS[0]}
          set -e

          if [ "$rc" -eq 0 ]; then
            VERSION=$(grep '^version' python/pyproject.toml | head -1 | sed 's/.*"\(.*\)"/\1/')

            # Keep rust/Cargo.lock in sync with the new crate version and fold it
            # into the bump commit so the lockfile and tag stay consistent.
            ( cd rust && cargo update -p inquirer-ai --precise "${VERSION}" )
            if ! git diff --quiet -- rust/Cargo.lock; then
              git add rust/Cargo.lock
              git commit --amend --no-edit --no-verify
              # Re-point the tag at the amended commit.
              git tag -f "v${VERSION}"
            fi

            # Push the branch and tag FIRST; only set outputs once both succeed.
            git push origin "HEAD:${GITHUB_REF_NAME}"
            git push origin "v${VERSION}"

            echo "version=v${VERSION}" >> "$GITHUB_OUTPUT"
            echo "bumped=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" -eq 21 ] || [ "$rc" -eq 3 ]; then
            # Not an error — nothing to release this run:
            #   21 == NoneIncrementExit: commits exist but none bump (build/test/...).
            #    3 == NO_COMMITS_FOUND: no new commits since the last tag — this is
            #         what the "bump:" commit's own push to main produces, so the
            #         re-triggered Release run must treat it as a clean no-op.
            echo "No version bump needed (nothing to bump since last tag)"
            echo "bumped=false" >> "$GITHUB_OUTPUT"
          else
            echo "cz bump failed with exit code ${rc}" >&2
            exit "$rc"
          fi

  release:
    needs: [auto-bump]
    if: always() && (needs.auto-bump.outputs.bumped == 'true' || (needs.auto-bump.result == 'skipped' && github.ref_type == 'tag'))
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ needs.auto-bump.outputs.version || github.ref_name }}

      - name: Generate changelog
        uses: orhun/git-cliff-action@v4
        id: changelog
        with:
          config: cliff.toml
          args: --latest --strip header

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.auto-bump.outputs.version || github.ref_name }}
          body: ${{ steps.changelog.outputs.content }}

  publish-pypi:
    needs: [auto-bump]
    if: always() && (needs.auto-bump.outputs.bumped == 'true' || (needs.auto-bump.result == 'skipped' && github.ref_type == 'tag'))
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.auto-bump.outputs.version || github.ref_name }}

      - uses: astral-sh/setup-uv@v7

      - name: Build
        run: uv build --out-dir dist/

      - name: Publish to PyPI
        # Idempotent: a single bumped release fires this job from both the branch
        # push and the tag push, so skip if the version is already on PyPI.
        run: |
          VERSION=$(grep '^version' pyproject.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          if curl -fsSo /dev/null "https://pypi.org/pypi/inquirer-ai/${VERSION}/json"; then
            echo "inquirer-ai ${VERSION} already on PyPI — skipping."
          else
            uv publish dist/*
          fi
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_TOKEN }}

  publish-npm:
    needs: [auto-bump]
    if: always() && (needs.auto-bump.outputs.bumped == 'true' || (needs.auto-bump.result == 'skipped' && github.ref_type == 'tag'))
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: typescript
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.auto-bump.outputs.version || github.ref_name }}

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npx tsc

      - name: Publish to npm
        # Idempotent — skip if this version is already on npm (see PyPI note).
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if [ -n "$(npm view "inquirer-ai@${VERSION}" version 2>/dev/null)" ]; then
            echo "inquirer-ai ${VERSION} already on npm — skipping."
          else
            npm publish
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-crates:
    needs: [auto-bump]
    if: always() && (needs.auto-bump.outputs.bumped == 'true' || (needs.auto-bump.result == 'skipped' && github.ref_type == 'tag'))
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: rust
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.auto-bump.outputs.version || github.ref_name }}

      - uses: dtolnay/rust-toolchain@stable

      - name: Publish to crates.io
        # Idempotent — skip if this version is already on crates.io (see PyPI note).
        run: |
          VERSION=$(grep '^version' Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          if curl -fsSo /dev/null -H "User-Agent: inquirer-ai-release" "https://crates.io/api/v1/crates/inquirer-ai/${VERSION}"; then
            echo "inquirer-ai ${VERSION} already on crates.io — skipping."
          else
            cargo publish --allow-dirty
          fi
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_TOKEN }}
